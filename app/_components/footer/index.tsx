import React from 'react';
import Box from "@mui/material/Box";

const Footer = () => {
    return (
        <Box component={"footer"}>
            Footer :)
        </Box>
    );
};

export default Footer;
